def token_t enum {
    k_begin,
    // ...
    k_end;

    def string proc(self): string {
        if tokens.find(self) != nil {
            return tokens[self];
        }
        return "TOKEN(" + string(self) + ")";
    };
};
